// action constants
const GET_USER = 'GET_USER';

// action creators

// thunk creators

const initialState = {
	name: 'Jake',
	favoriteHobby: 'banjo'
};

function userReducer(state = initialState, action) {
	return state;
}

export default userReducer;
